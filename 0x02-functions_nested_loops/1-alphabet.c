#include "main.h"

/**
 * print_alphabet - prints all alphabets in lowercase
 *
 * rturn (0); success
 */
void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	_putchar(ch);
	_putchar('\n');
}
