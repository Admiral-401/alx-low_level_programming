#include "main.h"

/**
 * _strlen - check length of string
 * @s: gets the length of string
 *
 * Return: length of string Always success;
 **/

int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;
	return (length);
}
