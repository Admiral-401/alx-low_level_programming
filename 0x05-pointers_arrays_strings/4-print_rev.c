#include "main.h"

/**
 * print_rev - prints a string on reverse
 * @s: Receives reversed sting
 *
 * Return: void
 *
 **/

void print_rev(char *s)
{
	int ch = 0;

	while (s[ch] != '\0')
	{
		ch++;
	}
	for (c - = 1; c > = 0; c--)
	{
		_putchar(s[ch]);
	}
	_putchar('\n');
}
