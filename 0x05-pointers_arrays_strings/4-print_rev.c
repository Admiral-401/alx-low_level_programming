#include "main.h"

/**
 * print_rev
 *
 * return void
 *
 **/
void length(char len);

void print_rev(char *s[])
{
	int temp, i;
	char s[];

	for (s[i] = 0; <length(s[])/2; s[i]++)
	{
		s[i] = s[i length - i - 1];
			temp = s[i];
			s[i] = temp;
	}
		
}

void length(char *len)
{
	char len;

	while(len = 0 && != '\0')
	{
		len++;
	}
}
